#include <cstdlib>
#include <iostream>
#include <ctime>

using namespace std;

int main(){
    int tab[6][6];
        srand(time(NULL));


    for(int j=0; j<6; j++)
    {
        for(int d=0; d<6; d++)
        {
            tab[j][d] = rand() % 34 + 1;
            cout << tab[j][d] << "\t";
        }
        
        for(int j = 0; j < 6; j++)
        {
            for(int d = 0; d < 0; d++)
            {
                tab[j][d] = rand()% 34 + 1; 
                cout << tab[j][d] << "\t";
        }
        cout << endl;
    }
    
        cout << endl;
    }

    int dzialanie;
    int napad;

    cout << "wybierz działanie" << endl;
    cin >> dzialanie;

    switch(dzialanie)
    {
        case 1:
            cout << "To jest napad, dawaj liczbę >:(" << endl;
            cin >> napad;
    
            for(int j=0; j<6; j++)
            {
                for(int d=0; d<6; d++)
                {
                    cout << tab[j][d] + napad << "\t";
                }
                cout << endl;
            }
            break;
            
        case 2:
            cout << "To jest napad, dawaj liczbę >:(" << endl;
            cin >> napad;
    
            for(int j=0; j<6; j++)
            {
                for(int d=0; d<6; d++)
                {
                    cout << tab[j][d] * napad << "\t";
                }
                cout << endl;
            }
            break;
        default: 
            cout << "ta wartość nie jest poprawna" << endl;
    }
}